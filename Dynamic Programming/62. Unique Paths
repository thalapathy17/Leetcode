class Solution {
    int[][] dp;
    public int uniquePaths(int m, int n) {
        
        dp = new int[m][n]; 
        return helper(m-1, n-1);
    }

    private int helper(int x, int y){
        if(x == 0 & y == 0) return 1;
        if(x < 0 || y < 0){
            return 0;
        }
        if(dp[x][y] != 0){
            return dp[x][y];
        }

        int res = helper(x - 1, y) + helper(x, y - 1);
        dp[x][y] = res;
        return res;
    }
}
